<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>GIT on Cee Yang</title><link>https://ceeyang.com/tags/git/</link><description>Recent content in GIT on Cee Yang</description><image><title>Cee Yang</title><url>https://ceeyang.com/images/og-cover.png</url><link>https://ceeyang.com/images/og-cover.png</link></image><generator>Hugo</generator><language>zh-cn</language><lastBuildDate>Sun, 25 Jun 2017 00:00:00 +0000</lastBuildDate><atom:link href="https://ceeyang.com/tags/git/index.xml" rel="self" type="application/rss+xml"/><item><title>Git 快速入门教程</title><link>https://ceeyang.com/posts/2017-06-25-git-%E5%BF%AB%E9%80%9F%E5%85%A5%E9%97%A8%E6%95%99%E7%A8%8B/</link><pubDate>Sun, 25 Jun 2017 00:00:00 +0000</pubDate><guid>https://ceeyang.com/posts/2017-06-25-git-%E5%BF%AB%E9%80%9F%E5%85%A5%E9%97%A8%E6%95%99%E7%A8%8B/</guid><description>Git 快速入门教程</description><content:encoded><![CDATA[<h1 id="git-使用教程">Git 使用教程</h1>
<h2 id="一git-的安装">一、Git 的安装</h2>
<h6 id="windows版本-">Windows版本 :</h6>
<p>直接前往git官网下载<a href="http://msysgit.github.io/">msysgit安装包</a>进行安装即可，安装流程和一般的windows软件的安装流程差不多，没什么区别。</p>
<h6 id="mac版本">Mac版本:</h6>
<p>Mac已经自带了git，无需安装。</p>
<h2 id="二配置git提交的用户名和邮件">二、配置Git提交的用户名和邮件</h2>
<p>这样做的目的是为了在git的log日志里面可以清楚的区分出每次的提交人是谁，以防日后出了问题能够立马清晰准确的定位出是哪个屌丝挖的坑，操作步骤：</p>
<h5 id="打开-git-控制台">打开 git 控制台：</h5>
<ul>
<li>Windows：随便在哪个空白位置右键，然后 <code>git bash</code>，调出git的命令控制台。</li>
<li>Mac：随便打开一个你喜欢的终端即可(<strong>Terminal</strong>, <strong>Iterm2</strong> 等)。</li>
</ul>
<h5 id="然后输入以下命令">然后输入以下命令：</h5>
<pre tabindex="0"><code>$ git config --global user.name &#34;Your Name（用户名）&#34;
$ git config --global user.email &#34;email@example.com（邮箱）&#34;
</code></pre><p>这里简单的说明一下，加上<code>--global</code>参数是表示配置的全局范围，针对所有的git项目而言的，当然也可以分别给某一个项目配置，进入到某个项目里面，去掉<code>–-global</code>参数即可。</p>
<h2 id="三生成-sshkey">三、生成 SSHKey</h2>
<p>Git推荐使用SSH协议传输文件（代码），会生成一个公钥和私钥，公钥配置在服务端，每次可以省去验证，方便快捷。当然使用https协议也是可以的，只不过是每次都需要输入用户名和密码，相对来说比较繁琐。</p>
<h5 id="步骤">步骤:</h5>
<p>任意空白处右键调出<code>git Bash</code>命令控制台，如果是Mac系统，打开终端就行了，输入以下命令：</p>
<pre tabindex="0"><code>$ ssh-keygen -t rsa -C &#34;youremail@example.com&#34;
</code></pre><p>然后一路回车，使用默认值即可，应该是需要3个回车的，设置密码为空(注意按3个回车的时候别输入其他的，不然设置的密码不为空，你就等着每次提交输入密码吧)。</br>
如果一切顺利的话，可以在用户主目录里找到.ssh目录，里面有 <strong>id_rsa</strong> 和 <strong>id_rsa.pub</strong> 两个文件，这两个就是SSH Key的秘钥对，<strong>id_rsa</strong> 是私钥，不能泄露出去，<strong>id_rsa.pub</strong> 是公钥，可以放心地告诉任何人。</br>
一般这个 <strong>id_rsa.pub</strong> 里面的内容是要配到git服务器上面的（由统一的人去管理），然后我们就可以从服务器上面拿代码和提交代码了。Mac系统默认目录为<code>~/.ssh</code>。</p>
<h2 id="四获取代码与提交代码">四、获取代码与提交代码</h2>
<p>首先给大家介绍几个概念，一个是本地 <strong>worksapce</strong> 工作区，一个是 <strong>stash</strong> 暂存区，一个是 <strong>localRepository</strong> 本地库，一个是 <strong>remoteRepository</strong> 远程库。</p>
<ul>
<li>从git服务器下载代码到本地</li>
</ul>
<pre tabindex="0"><code>git clone git@github.com:ceeyang/ceeyang.github.io.git
</code></pre><ul>
<li>查看本地分支和远程分支，其中<code>-a</code>参数查看远程分支</li>
</ul>
<pre tabindex="0"><code>git branch
git branch -a
</code></pre><ul>
<li>创建本地分支</li>
</ul>
<pre tabindex="0"><code>git brnach newBranch
</code></pre><ul>
<li>切换本地分支</li>
</ul>
<pre tabindex="0"><code>git checkout newBranch
</code></pre><ul>
<li>创建本地新分支并切换到该分支</li>
</ul>
<pre tabindex="0"><code>git checkout -b newBranch
</code></pre><ul>
<li>基于本地分支创建远程分支</li>
</ul>
<pre tabindex="0"><code>git push origin newBranch
</code></pre><ul>
<li>删除分支（注意：删除分支需要先切换到其他的分支，如<code>master</code>分支，再执行删除操作）</li>
</ul>
<pre tabindex="0"><code>git checkout master
git branch -d dev
</code></pre><ul>
<li>创建分支并且切换分支
基于远程分支创建本地分支并且切换到该本地分支，基于远程 dev 分支在本地创建一个 dev 分支，并且切换到 dev 分支。</li>
</ul>
<pre tabindex="0"><code>// 1
git checkout -b  newBranch origin/newBranch  
// 2
git checkout -b newBranch
// 3
git checkout newBranch
</code></pre><ul>
<li>暂存本地工作区的修改</li>
</ul>
<pre tabindex="0"><code>git add README.MD
</code></pre><p>通常使用用（&quot;<code>.</code>&ldquo;代表所有文件，注意&rdquo;<code>.</code>&ldquo;前面的空格）</p>
<pre tabindex="0"><code>git add .
</code></pre><ul>
<li>提交暂存区的内容到本地库中</li>
</ul>
<pre tabindex="0"><code>git commit -m &#34;注释&#34;
</code></pre><ul>
<li>抓取远程库最新提交的代码,</li>
</ul>
<pre tabindex="0"><code>git fetch origin dev
</code></pre><p>或者：</p>
<pre tabindex="0"><code>git fetch
</code></pre><p><code>git fetch</code>是简写(git 会自动搜索和当前分支名相同的远程分支名)</p>
<ul>
<li>将本地修改与远程进行合并此时，若遇到冲突，需要先手动解决冲突，再执行合并。</li>
</ul>
<pre tabindex="0"><code>git rebase origin/dev
</code></pre><p>这个时候如果出现了冲突，就先解决冲突，冲突解决完之后，需先缓存到本地工作区，然后执行合并。</p>
<pre tabindex="0"><code>git add .
git rebase --continue
</code></pre><ul>
<li>安装changeId自动生成hook
 
注意：如果使用了<code>gerrit</code>代码审查工具，提交的时候就必须要先生成这个<code>changID</code>值，如果没有这个<code>changID</code>,则不能到<code>gerrit</code>代码审核工具中进行代码审核操作了。而<code>gerrit</code>代码审查工具的存在就没有意义了。
操作步骤：进入到工作空间（<code>workspace</code>）中的仓库目录，输入如下命令：</li>
</ul>
<pre tabindex="0"><code>scp -p -P 29418 username@www.dreamtech.com.cn:hooks/commit-msg .git/hooks/
</code></pre><ul>
<li>将本地仓库中的代码push推送到远程仓库中
这里需要注意：
我们也可以直接用命令<code>git push origin dev</code>将代码直接<code>push</code>到远程代码仓库，但是由于现在我们这边是使用了<code>gerrit</code>代码审查工具，所有不能直接这样推送，必须使用<code>git push origin head:refs/for/dev</code> 这个命令将代码推送到<code>gerrit</code>服务器上，然后我们再登录到<code>gerrit</code>中（会有一个web页面供我们操作）进行审核之后，执行最后合并，此时如果报错的话 有可能是因为你的<code>changID</code>没有生成，使用之前介绍的命令生成<code>changID</code>。</li>
</ul>
<pre tabindex="0"><code>git push origin HEAD:refs/for/dev
</code></pre><p>或者:</p>
<pre tabindex="0"><code>git push origin head:refs/for/dev
</code></pre><ul>
<li>登录到<code>gerrit</code>服务器中，进行代码审核，然后执行合并（这一步先不管，专人统一审核，合并提交代码到远程中央仓库）</li>
</ul>
<h2 id="五撤销修改">五、撤销修改</h2>
<ul>
<li>场景一：当你改乱了工作区某个文件的内容，想直接丢弃工作区的修改时，使用</li>
</ul>
<pre tabindex="0"><code>git checkout -- filename
</code></pre><p>通常用（&rdquo;.&ldquo;代表所有文件，注意&rdquo;.&ldquo;前面的空格）</p>
<pre tabindex="0"><code>git checkout -- .
</code></pre><ul>
<li>场景二：当你不但改乱了工作区的某个文件的内容，而且还提交到了暂存区（在本地执行了<code>commit</code>提交，但是还没有<code>commit</code>提交到远程服务器）时，想丢弃修改，分两步：</li>
</ul>
<p>第一步: 使用命令:</p>
<pre tabindex="0"><code>git reset HEAD filename
</code></pre><p>或者使用<code>.</code>代替全部：</p>
<pre tabindex="0"><code>git reset HEAD .
</code></pre><p>就又回到了场景一.</p>
<p>第二步: 再按照场景一的步骤操作.</p>
<ul>
<li>场景三：已经提交了和不合适的修改带版本库时，想要撤销本次修改，则可以使用版本回退，但是前提是还没有推送push到远程库</li>
</ul>
<p>版本回退见下面介绍</p>
<h2 id="六版本回退">六、版本回退</h2>
<p><code>HEAD</code>指向的版本就是当前版本，因此，Git允许我们在版本的历史之间穿梭，使用命令</p>
<pre tabindex="0"><code>git  reset  --soft commit_id
</code></pre><p>或者</p>
<pre tabindex="0"><code>git reset --soft HEAD^
</code></pre><p>穿梭前，用<code>git log</code>可以查看提交历史，以便确定要回退到哪个版本。
要重返未来，用<code>git reflog</code>查看命令历史，以便确定要回到未来的哪个版本。</p>
<hr>
<h2 id="七stash-暂存">七、Stash 暂存</h2>
<p>关于<code>git Stash</code>的详细解释，适用场合，这里做一个说明：
当前工作区内容已被修改，但是并未完成。这时 Boss 来了，说前面的分支上面有一个 Bug ，需要立即修复。可是我又不想提交目前的修改，因为修改没有完成。但是，不提交的话，又没有办法<code>checkout</code>到另外的分支。此时用<code>git Stash</code>就相当于备份当前工作区了。然后在<code>Checkout</code>过去修改，就能够达到保存当前工作区，并及时恢复的作用。
之后，另一个版本的 bug 修复完毕之后，再使用<code>checkout</code>切换回之前的分支，然后再使用<code>git stash pop</code>命令或者<code>git stash apply</code>命令就又可以恢复回来了。</p>
<pre tabindex="0"><code>git stash save &#34;备注说明&#34;
</code></pre><p>备份当前的工作区的内容，从最近的一次提交中读取相关内容，让工作区保证和上次提交的内容一致。同时，将当前的工作区内容保存到Git栈中。</p>
<pre tabindex="0"><code>git stash pop stashId
</code></pre><p>从Git栈中读取最近一次保存的内容，恢复工作区的相关内容。由于可能存在多个<code>Stash</code>的内容，所以用栈来管理，<code>pop</code>会从最近的一个stash中读取内容并恢复。</p>
<pre tabindex="0"><code>git stash list
</code></pre><p>显示Git栈内的所有备份，可以利用这个列表来决定从那个地方恢复。</p>
<pre tabindex="0"><code>git stash clear
</code></pre><p>清空Git栈。此时使用gitg等图形化工具会发现，原来stash的哪些节点都消失了。</p>
<h2 id="八git如何放弃所有本地修改">八、git如何放弃所有本地修改</h2>
<pre tabindex="0"><code>git checkout .
</code></pre><p>放弃本地所有修改的。没有的提交的，都返回到原来的状态 </p>
<pre tabindex="0"><code>git stash .
</code></pre><p>把所有没有提交的修改暂存到<code>stash</code>里面。可用<code>git stash pop</code>恢复。 </p>
<pre tabindex="0"><code>git reset --hard HASH
</code></pre><p>返回到某个节点，不保留修改。</p>
<pre tabindex="0"><code>git reset --soft HASH
</code></pre><p>返回到某个节点。保留修改</p>
<h2 id="九git只更新远程库中的单个或者指定的文件">九、Git只更新远程库中的单个或者指定的文件</h2>
<p>在开发过程中，我们有的时候并不需要更新远程所有的文件，而只需要更新某一个或者说特定的文件，如何操作呢？
<strong>SVN</strong> 更新单个文件只需要<code>svn up file/to/updat</code>，非常简单，而且没有任何歧义。
但是<strong>git</strong>由于在远程服务器和本地都有一个代码库，这样只更新单个文件的话比<strong>SVN</strong>稍微要麻烦一点。</p>
<ul>
<li>如果想拿远程git服务器上的最新版本（或者某个特定版本）去覆盖本地的修改，可以使用<code>git pull</code>命令，但是这样会全面更新本地代码库和工作拷贝。</li>
<li>如果想放弃本地工作拷贝所做的修改，可以使用 <code>git checkout filePath</code>命令，但该命令只能用本地库覆盖你的工作拷贝，并不能取得远程版本的更新。
所以，正确的方法应该是先更新本地库（但是不更新工作拷贝），然后用本地库来更新单个的工作拷贝文件。</li>
</ul>
<blockquote>
<p>例如：假设说开发小伙A在本地新增或者是修改了某个文件file1,然后提交到远程库了，之后开发小伙B想要拿到这个file1文件，（注意场景：这里是只想拿到这一个文件file1，小伙A有可能提交了多个文件），那么小伙B这边可以这么做：</p>
</blockquote>
<blockquote>
<ol>
<li>第一步，抓取远程库最新修改：<code>git fetch</code></li>
<li>第二步，指定某个文件进行更新: <code>git checkout origin/dev -- filePath</code></li>
</ol>
</blockquote>
<h2 id="十分支的合并">十、分支的合并</h2>
<p>假设我们当前的版本有一个<code>master</code>主干分支，同时还有一个<code>dev</code>开发分支，因为通常情况下我们做开发都是在<code>dev</code>分支下进行的开发，之后开发完成后需要将<code>dev</code>分支开发的功能同步到<code>master</code>主干分支上，也就是合并到<code>master</code>分支上，那么怎么操作呢？</p>
<p>1.首先切换到需要合并的分支上，打个比方说：现在我们需要将<code>dev</code>分支合并到<code>master</code>主干分支，第一步：首先我们需要切换到<code>master</code>主干分支上 :</p>
<pre tabindex="0"><code>git checkout master
</code></pre><p>2.为了保证当前的本地<code>master</code>分支的内容与远程服务器中<code>master</code>分支的内容保持同步，我们需要先同步代码:</p>
<pre tabindex="0"><code>git pull origin master
</code></pre><p>通常我们会拆成两个步骤来完成：</p>
<pre tabindex="0"><code>git fetch origin master
</code></pre><p>或者</p>
<pre tabindex="0"><code>git fetch
git rebase origin/master
</code></pre><p>3.将dev分支合并到当前master分支</p>
<pre tabindex="0"><code>git merge dev
</code></pre><p>或者</p>
<pre tabindex="0"><code>git merge --no-ff dev
</code></pre><p>4.此时如果在合并的过程中出现了冲突，则需要先解决冲突，解决方法同 <strong>svn</strong> 差不多</p>
<p>5.冲突解决完之后，再继续执行合并操作</p>
<pre tabindex="0"><code>git add -A
git rebase --continue
</code></pre><p>6.合并成功之后，我们再将master分支push推送到远程库中即可</p>
<pre tabindex="0"><code>git push origin master
</code></pre><hr>
<h2 id="十一如何合并某个分支中的一个或者是几个commits提交">十一、如何合并某个分支中的一个或者是几个commit（s）提交</h2>
<p><code>git cherry-pick</code>可以选择某一个分支中的一个或者是几个<code>commit(s)</code>来进行合并操作。在实际应用中，我们可能会遇到这样的情况：</p>
<p>假设现在我们有个稳定版本的分支叫做<code>release</code>，另外还有一个开发版本的分支叫做<code>dev</code>，然后我们在<code>dev</code>开发分支里面修改了一些东西（新增了几个个功能，或者是修复了几个bug等等），之后 <strong><code>commit</code></strong> 了几个（这里注意是几个）更新操作（也就是提交到了本地库），但是后来我们发现同样需要在<code>release</code>分支上同步应用某个更新，我们不能直接将<code>release分</code>支和<code>dev</code>分支执行合并，因为这样将会导致稳定版本混乱，但是确实又想增加一个<code>dev</code>分支中的某个功能到<code>release</code>分支中来，那么这时我们就可以使用<code>cherry-pick</code>命令来解决这个问题了，就是对已经存在的commit进行再次<code>commit</code>（提交）操作。简单用法：</p>
<pre tabindex="0"><code>git cherry-pick &lt;commit_id&gt;
</code></pre><p>注意：当执行完<code>cherry-pick</code>以后，将会生成一个新的提交，这个新的提交的哈希值和原来的不一致，但标识名是一样的。</p>
<p>1.首先，切换到dev开发分支</p>
<pre tabindex="0"><code>git checkout dev
</code></pre><p>2.然后找到<code>dev</code>分支的<strong>commitId 38361a6</strong>（说明：本来commitId是38361a68138140827b31b72f8bbfd88b3705d77a这么多位，但是可以只取前7位就行了。使用<code>git log</code>命令可以查看提交的 <strong>commitId</strong>）</p>
<pre tabindex="0"><code>git log
commit 38361a68138140827b31b72f8bbfd88b3705d77a
</code></pre><p>3.之后再次切换回 <strong>release</strong> 分支</p>
<pre tabindex="0"><code>git checkout release
</code></pre><p>4.更新一下 <strong>release</strong> 版本的代码，保持与远程仓库的同步</p>
<pre tabindex="0"><code>git fetch origin release
git rebase orgin/release
</code></pre><p>5.执行<code>cherry-pick</code>命令，同步 <strong>dev</strong> 分支的修改代码</p>
<pre tabindex="0"><code>git cherry-pick 38361a6
</code></pre><p>6.如果顺利，就会出现下面这段提示，当然也有可能不出现这段提示，只要没报冲突，则说明代码同步成功了。</p>
<pre tabindex="0"><code>Finished one cherry-pick
# On branch old_cc
# Your branch is ahead of &#39;origin/old_cc&#39; by 3 commits.
</code></pre><p>7.如果在<code>cherry-pick</code>的过程中出现了冲突就手动解决冲突，解决完冲突之后根据提示继续执行同步。</p>
<pre tabindex="0"><code>git add -A 
git cherry-pick --continue
</code></pre><p>8.最后push到远程仓库。</p>
<pre tabindex="0"><code>git push origin release
</code></pre><hr>
<h2 id="十二一个完整的团队开发流程">十二、一个完整的团队开发流程</h2>
<p>1.克隆远程仓库</p>
<pre tabindex="0"><code>git clone ssh://xxx@www.dreamtech.com:12256/xxx
</code></pre><p>2.切换到对应分支:<code>git checkout dev</code>、<code>git checkout -b dev</code> or <code>git checkout -b dev origin/dev</code></p>
<pre tabindex="0"><code>git checkout -b dev origin/dev
</code></pre><p>3.查看当前分支状态，查看提交。</p>
<pre tabindex="0"><code>git status
git log
</code></pre><p>4.添加 <strong>commit msg</strong> ,一个项目只需要添加一次即可。</p>
<pre tabindex="0"><code>scp -p -P 29418 username@www.dreamtech.com.cn:hooks/commit-msg .git/hooks/
</code></pre><p>5.当本地修改后，缓存本地文件。添加 commit msg。</p>
<pre tabindex="0"><code>git add -A 或者 git add .
git commit -m &#34;注释&#34;
</code></pre><p>6.抓取远程提交，合并本地代码。</p>
<pre tabindex="0"><code>git fetch origin dev
git rebase origin/dev
</code></pre><p>这个时候如果出现冲突，注意出现冲突只可能会是在合并(<code>rebase</code>)时出现冲突，那我们就直接解决冲突即可，解决完之后,再依次执行：</p>
<pre tabindex="0"><code>git add -A
git rebase --continue
</code></pre><p>9.推送到远程仓库。</p>
<pre tabindex="0"><code>git push origin HEAD:refs/for/dev
</code></pre><p>或者如果远程库（<code>gerrit</code>仓库）只是一个空的仓库的话，这就需要先将本地库的代码关联到<code>gerrit</code>远程库（即将本地库与远程库建立映射关系），之后再推送上去。执行以下命令：</p>
<pre tabindex="0"><code class="language- " data-lang=" ">git remote add origin ssh://xxx@www.dreamtech.com:12256/xxx
git push -u origin HEAD:refs/for/master
</code></pre><p>这里加了<code>-u</code>参数，首次推送的话需要加上这个-u参数，以后都不需要了。</p>
]]></content:encoded></item></channel></rss>